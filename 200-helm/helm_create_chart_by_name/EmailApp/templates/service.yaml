apiVersion: v1
kind: Service
metadata:
  name: {{ include "EmailApp.fullname" . }}
  labels:
    {{- include "EmailApp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "EmailApp.selectorLabels" . | nindent 4 }}
