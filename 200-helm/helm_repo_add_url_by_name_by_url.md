# helm_repo_add_url_by_name_by_url

## Function Call:

```bash
helm_repo_add_url_by_name_by_url bitnami https://charts.bitnami.com/bitnami
```

## Description

Adds a Helm chart repository

## Actual call

```bash
helm repo add bitnami https://charts.bitnami.com/bitnami
```

## output:

```
"bitnami" already exists with the same configuration, skipping
```
